apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring6restmvc.fullname" . }}
  labels:
    {{- include "spring6restmvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "spring6restmvc.selectorLabels" . | nindent 4 }}
