apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring6restmvc.fullname" . }}-test-connection"
  labels:
    {{- include "spring6restmvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring6restmvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
